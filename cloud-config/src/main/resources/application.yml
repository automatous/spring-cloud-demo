server:
  port: 10010
spring:
  application:
    name: cloud-config-hi
  cloud:
    config:
      server:
        git:
          uri: https://github.com/automatous/cloud_config_rep.git
          default-label: master